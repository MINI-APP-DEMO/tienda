import { Component, ReactNode } from "react";

export class Dashboard extends Component<any, any> {
 constructor(props: any) { super(props); this.state = {} }
 render(): ReactNode {
  return <>ES LA PAGINA DEL DASHBOARD</>
 }
}